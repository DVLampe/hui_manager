# Документация по дизайну и компонентам

В этом документе описываются страницы и элементы пользовательского интерфейса приложения.

---

## 1. Страница "Дашборд" (`/dashboard`)

Основная страница, предоставляющая пользователю сводную информацию о его финансовой деятельности и участии в "hụi".

### 1.1. Заголовок страницы

- **Элемент:** `<h1>`
- **Текст:** "Bảng thống kê" (Статистика)
- **Описание:** Главный заголовок страницы.

### 1.2. Блок сводных карточек (Stats Cards)

Набор информационных карточек, отображающих ключевые финансовые показатели.

- **Карточка 1:**
  - **Название:** "Tổng tiền đã trả" (Всего выплачено)
  - **Данные:** Отображает общую сумму, выплаченную пользователем.
- **Карточка 2:**
  - **Название:** "Tổng tiền đã nhận" (Всего получено)
  - **Данные:** Отображает общую сумму, полученную пользователем.
- **Карточка 3:**
  - **Название:** "Tổng tiền thảo" (Всего "thảo")
  - **Данные:** Отображает общую сумму "thảo".
- **Карточка 4:**
  - **Название:** "Lợi nhuận" (Прибыль/Убыток)
  - **Данные:** Отображает общую прибыль или убыток.
- **Карточка 5:**
  - **Название:** "Tổng số hụi" (Всего hụi)
  - **Данные:** Отображает общее количество hụi, в которых пользователь принимал участие.
- **Карточка 6:**
  - **Название:** "Số hụi đang tham gia" (Hụi, в которых участвует)
  - **Данные:** Отображает количество активных hụi.

### 1.3. Блок графика (Chart)

Визуальное представление финансовых данных во времени.

- **Элементы управления:**
  - **Выпадающий список (Select):** Позволяет группировать данные на графике по дням ("Ngày"), месяцам ("Tháng") или годам ("Năm").
- **Содержимое:**
  - **Линейный график:** Динамически загружаемый компонент, отображающий статистику. Имеет состояние загрузки.

### 1.4. Блок списков Hụi (Hui Lists)

Секция для отображения списков hụi, в которых пользователь является участником или владельцем.

- **Элементы управления:**
  - **Вкладки (Tabs):** Две кнопки-вкладки для переключения между списками:
    - "Hụi tham gia" (Hụi, в которых участвует)
    - "Hụi làm chủ" (Hụi, которыми владеет)
- **Содержимое:**
  - **Компонент `HuiList`:** Таблица со списком hụi.
    - **Кнопка "Export":** При нажатии открывает выпадающее меню с двумя опциями:
      - **"Export as PDF":** Экспортирует данные таблицы в PDF-файл.
      - **"Export as Excel":** Экспортирует данные таблицы в XLSX-файл.
    - **Таблица:** Содержит колонки: "Tên hụi" (Название), "Trạng thái" (Статус), "Số tiền" (Сумма) и др.
- **Интерактивность:** Каждая строка таблицы является кликабельной и ведет на страницу с детальной информацией о соответствующем hụi (`/hui/[id]`).
- **Отображаемые данные:** В зависимости от типа списка (`участвует` или `владеет`), в последней колонке отображается либо "Lợi nhuận/Thua lỗ" (Прибыль/Убыток), либо "Tổng tiền thảo" (Общая сумма "thảo").

---

## 2. Аутентификация

Блок страниц, отвечающих за вход, регистрацию и восстановление доступа к аккаунту. Все страницы используют общий минималистичный макет с формой по центру.

### 2.1. Страница входа (`/signin`)

- **Заголовок:** "Đăng nhập vào tài khoản" (Вход в аккаунт).
- **Форма:**
  - **Поле "Địa chỉ email" (Email):** Стандартное поле для ввода email.
  - **Компонент `PasswordInput` (Пароль):** Поле для ввода пароля с возможностью скрыть/показать символы.
- **Ссылки:**
  - **"Quên mật khẩu?" (Забыли пароль?):** Ведет на страницу `/forgot-password`.
- **Кнопки:**
  - **"Đăng nhập" (Войти):** Основная кнопка для отправки формы. Имеет состояние загрузки.
- **Дополнительные элементы:**
  - **Компонент `SocialLogins`:** Блок для входа через социальные сети (например, Google, Facebook).
  - **Ссылка на регистрацию:** Текст "Chưa có tài khoản? Đăng ký ngay" (Нет аккаунта? Зарегистрироваться) ведет на страницу `/register`.

### 2.2. Страница регистрации (`/register`)

- **Заголовок:** "Tạo tài khoản mới" (Создать новый аккаунт).
- **Форма:**
  - **Поле "Họ và tên" (Имя и фамилия):** Поле для ввода полного имени.
  - **Поле "Địa chỉ email" (Email):** Поле для ввода email.
  - **Компонент `PasswordInput` (Пароль):** Поле для ввода нового пароля.
- **Кнопки:**
  - **"Đăng ký" (Зарегистрироваться):** Основная кнопка для отправки формы. Имеет состояние загрузки.
- **Дополнительные элементы:**
  - **Компонент `SocialLogins`:** Блок для регистрации через социальные сети.
  - **Ссылка на вход:** Текст "Đã có tài khoản? Đăng nhập" (Уже есть аккаунт? Войти) ведет на страницу `/signin`.

### 2.3. Страница "Забыли пароль" (`/forgot-password`)

- **Заголовок:** "Quên mật khẩu" (Забыли пароль).
- **Подзаголовок:** Инструкция с просьбой ввести email для получения ссылки на сброс.
- **Форма:**
  - **Поле "Địa chỉ email" (Email):** Единственное поле для ввода email.
- **Кнопки:**
  - **"Gửi liên kết đặt lại" (Отправить ссылку для сброса):** Кнопка для отправки формы.
- **Дополнительные элементы:**
  - **Ссылка на вход:** Текст "Nhớ mật khẩu? Đăng nhập" (Вспомнили пароль? Войти) ведет на страницу `/signin`.
  - **Уведомление об успехе:** После отправки отображается сообщение о том, что ссылка была отправлена (если пользователь с таким email существует).

---

## 3. Страница "Список Hụi" (`/hui`)

Основная страница для просмотра всех hụi, в которых пользователь участвует или которыми управляет.

### 3.1. Заголовок и основное действие

- **Заголовок:** "Danh sách Hụi" (Список Hụi).
- **Кнопка "Tạo Hụi Mới" (Создать новый Hụi):** ведет на страницу `/hui/create`.

### 3.2. Панель фильтров и сортировки

- **Выпадающий список "Фильтр по статусу":** Позволяет фильтровать список по статусам "Tất cả trạng thái" (Все), "Đang hoạt động" (Активные), "Đang chờ" (В ожидании), "Đã đóng" (Закрытые).
- **Выпадающий список "Сортировка":** Позволяет сортировать список по дате следующего платежа, от ближайшего ("Gần nhất") к дальнему ("Xa nhất") и наоборот.

### 3.3. Основной контент

- **Сетка карточек:** Если hụi существуют, они отображаются в виде сетки компонентов `HuiCard`.
- **Состояние "пусто":** Если список пуст, отображается сообщение "Bạn chưa tham gia hoặc quản lý hụi nào" (Вы еще не участвуете и не управляете ни одним hụi) и кнопка "Tạo Hụi Đầu Tiên" (Создать первый Hụi).

### 3.4. Компонент `HuiCard`

Карточка для краткого представления одного hụi в списке.

- **Заголовок карточки:** Отображает имя hụi и его статус (`StatusBadge`).
- **Содержимое:**
  - **"Số tiền" (Сумма):** Сумма взноса.
  - **"Kỳ hiện tại" (Текущий период):** Отображает текущий период из общего числа (например, 5/12).
  - **"Thanh toán kế tiếp" (Следующий платеж):** Дата следующего платежа.
- **Кнопка "Xem chi tiết" (Посмотреть детали):** Кнопка внизу карточки, которая ведет на страницу с подробной информацией о hụi (`/hui/[id]`).

---

## 4. Страница "Детали Hụi" (`/hui/[id]`)

Страница с подробной информацией о конкретном hụi. Содержит множество интерактивных элементов для управления.

### 4.1. Заголовок и управляющие кнопки

- **Заголовок:** Название hụi.
- **Кнопки (для владельца/админа):**
  - **"Chỉnh sửa hụi" (Редактировать hụi):** Ведет на страницу редактирования.
  - **"Xóa hụi" (Удалить hụi):** Открывает модальное окно для подтверждения удаления.

### 4.2. Блок сводных карточек

- **Карточки:** Отображают ключевую информацию: "Số tiền mỗi kỳ" (Сумма за период), "Số thành viên" (Количество участников), "Chu kỳ" (Частота), "Trạng thái hụi" (Статус).
- **Индикатор прогресса:** Показывает, сколько периодов завершено из общего числа.

### 4.3. Блок основных действий

- **Кнопка "Hốt Hụi":** Открывает модальное окно для проведения розыгрыша.
- **Кнопка "Quay hụi" (Колесо фортуны):** Открывает модальное окно с колесом для случайного выбора участника.
- **Кнопка "QR chuyển khoản" (QR для перевода):** Открывает модальное окно с QR-кодом банка владельца.

### 4.4. Основной контент с вкладками

- **Вкладка "Thông tin chi tiết" (Детали):**
  - Отображает подробную информацию в виде списка: "Tên Hụi" (Название), "Ngày bắt đầu" (Дата начала), "Chủ Hụi" (Владелец), "Ngày kết thúc" (Дата окончания), "Mô tả" (Описание).
  - **Кнопка "Chỉnh sửa" (Редактировать):** Позволяет редактировать название и описание прямо на месте.
  - **Кнопка "Quản lý quyền" (Управление правами):** Открывает модальное окно для настройки прав доступа других пользователей.
- **Вкладка "Danh sách thành viên" (Список участников):**
  - Отображает компонент `MemberList` со списком всех участников.
  - **Кнопка "Thêm thành viên" (Добавить участника):** Ведет на страницу добавления участника.
- **Вкладка "Lịch thanh toán" (График платежей):**
  - Отображает компонент `PaymentScheduleTable` с общей таблицей платежей по периодам.
  - **Колонки таблицы:** "Kỳ" (Период), "Ngày đến hạn" (Срок), "Thành viên hốt hụi" (Кто забирает), "Thăm kêu", "Thảo", "Tiền hốt" (Сумма), "Trạng thái" (Статус).
  - **Кнопка "Chỉnh sửa" (Редактировать):** Позволяет редактировать данные по каждому периоду.
  - **Кнопка "Export":** Позволяет экспортировать таблицу в PDF/Excel.
- **Вкладка "Lịch chi tiết" (Детальный график):**
  - Отображает компонент `DetailedPaymentScheduleTable` с подробной разбивкой по каждому участнику за каждый период.
  - **Навигация:** Боковое меню для быстрого переключения между периодами.
  - **Детали периода:** Информация о том, кто забирает деньги, суммы "thăm kêu", "thảo" и итоговая.
  - **Таблица участников:** Список всех участников с указанием суммы взноса для каждого в выбранном периоде и их статуса ("hụi sống" / "hụi chết").
  - **Кнопка "Export":** Позволяет экспортировать детальный отчет в PDF/Excel.

### 4.5. Модальные окна

- **Модальное окно "Hốt Hụi":** Форма для проведения розыгрыша.
  - **Поля:** "Kỳ hốt" (Период), "Thành viên hốt" (Участник), "Thăm kêu", "Thảo".
  - **Кнопка "Xem trước hóa đơn" (Предпросмотр счета):** Генерирует и показывает счет-фактуру перед сохранением.
  - **Кнопка "Hốt không hóa đơn" (Провести без счета):** Сохраняет результат без генерации счета.
    - **Модальное окно "Hóa đơn" (Счет-фактура) после кнопки "Xem trước hóa đơn":**
      - Отображает детализированный счет для печати или сохранения.
      - **Кнопка "Tải xuống PDF" (Скачать PDF):** Генерирует и скачивает PDF-версию счета.

- **Модальное окно "QR chuyển khoản" (QR для перевода):**
  - "Ngân hàng" (Банк), "Tên tài khoản" (Имя на счете), "Số tài khoản" (Номер счета), "Mã QR" (QR-код)

- **Другие модальные окна:** Для удаления, управления правами, колеса фортуны.


### 4.6. Плавающая кнопка чата (`ChatModal`)

- **Иконка:** Иконка сообщения.
- **Действие:** При нажатии открывает модальное окно чата.
- **Окно чата:**
  - Содержит заголовок "Group Chat" и кнопку закрытия.
  - Внутри отображается компонент `Chat` с историей сообщений и полем для ввода нового.
  - **Поле ввода:** Содержит кнопку для выбора emoji (`EmojiPicker`).

---

## 5. Страница "Создание Hụi" (`/hui/create`)

Страница с формой для создания нового hụi.

### 5.1. Заголовок

- **Заголовок:** "Tạo Hụi Mới" (Создать новый Hụi).

### 5.2. Форма создания

- **Поле "Tên hụi" (Название):** Текстовое поле для названия.
- **Поле "Mô tả" (Описание):** Многострочное текстовое поле для описания.
- **Поле "Số tiền mỗi kỳ" (Сумма за период):** Числовое поле для суммы взноса.
- **Выпадающий список "Loại hụi" (Тип):** Выбор частоты взносов (День, Неделя, Месяц).
- **Поле "Ngày bắt đầu" (Дата начала):** Поле для выбора даты.
- **Поле "Ngày kết thúc" (Дата окончания):** Необязательное поле для выбора даты.
- **Поле "Số kỳ" (Количество периодов):** Числовое поле.

### 5.3. Блок "Добавление участников"

- **Заголовок:** "Thêm thành viên" (Добавить участников).
- **Компонент `AddMembersPanel`:** Интерактивная панель для поиска и добавления участников из списка друзей в создаваемый hụi.

### 5.4. Кнопки управления

- **Кнопка "Hủy" (Отмена):** Возвращает пользователя на страницу со списком hụi (`/hui`).
- **Кнопка "Tạo Hụi" (Создать Hụi):** Отправляет форму для создания нового hụi. Имеет состояние загрузки.

---

## 6. Страница "Друзья/Участники" (`/members`)

Страница для управления списком друзей и заявками в друзья.

### 6.1. Боковая панель навигации

- **Заголовок:** "Bạn bè" (Друзья).
- **Кнопка "Thêm bạn" (Добавить друга):** Открывает модальное окно для поиска и добавления новых друзей.
- **Вкладка "Danh sách bạn bè" (Список друзей):** Переключает основной контент на отображение списка друзей.
- **Вкладка "Lời mời kết bạn" (Запросы в друзья):** Переключает основной контент на отображение входящих и исходящих запросов.

### 6.2. Вкладка "Список друзей"

- **Заголовок:** "Danh sách bạn bè" (Список друзей) с указанием их количества.
- **Элементы управления:**
  - **Поле поиска:** Позволяет искать друзей по имени.
  - **Кнопка сортировки:** Сортирует список по имени от А до Я и наоборот.
- **Контент:** Список друзей, сгруппированный по алфавиту.

### 6.3. Вкладка "Запросы в друзья"

- **Блок "Lời mời đã nhận" (Полученные запросы):**
  - Список пользователей, отправивших запрос.
  - **Кнопка "Chấp nhận" (Принять):** Принимает запрос в друзья.
  - **Кнопка "Từ chối" (Отклонить):** Отклоняет запрос.
- **Блок "Lời mời đã gửi" (Отправленные запросы):**
  - Список пользователей, которым был отправлен запрос.
  - **Кнопка "Thu hồi lời mời" (Отозвать запрос):** Отменяет отправленный запрос.

### 6.4. Модальное окно "Добавить друга"

- **Заголовок:** "Thêm bạn" (Добавить друга).
- **Форма поиска:** Поле для поиска пользователей по email или номеру телефона.
- **Кнопка "Tìm kiếm" (Поиск):** Выполняет поиск.
- **Результаты поиска:** Список найденных пользователей.
- **Кнопка "Gửi lời mời" (Отправить запрос):** Отправляет запрос на добавление в друзья выбранному пользователю.

---

## 7. Страница "Профиль" (`/profile`)

Страница для просмотра и редактирования личной информации пользователя.

### 7.1. Заголовок профиля

- **Аватар:** Отображает изображение пользователя или иконку-заглушку.
  - **Интерактивность:** При наведении появляется иконка камеры. Нажатие открывает системное окно для выбора файла и последующее модальное окно `AvatarUploadModal` для обрезки и сохранения нового аватара.
- **Информация:** Отображает имя, email и роль пользователя (например, ADMIN).

### 7.2. Навигация по вкладкам

- **Вкладка "Thông tin cá nhân" (Личная информация):** Отображает личные и платежные данные.
- **Вкладка "Bảo mật" (Безопасность):** Содержит форму смены пароля.

### 7.3. Вкладка "Личная информация"

Имеет два режима: просмотр и редактирование.

- **Режим просмотра:**
  - **Личная информация:** "Họ và tên" (Полное имя), "Email", "Số điện thoại" (Телефон), "Ngày sinh" (Дата рождения), "Giới thiệu" (О себе), "Là thành viên từ" (Участник с).
  - **Платежная информация:** "Ngân hàng" (Банк), "Tên tài khoản" (Имя на счете), "Số tài khoản" (Номер счета), "Mã QR" (QR-код).
  - **Кнопка "Chỉnh sửa" (Редактировать):** Переключает вкладку в режим редактирования.
- **Режим редактирования:**
  - Все поля становятся доступными для редактирования.
  - Появляется поле для загрузки нового QR-кода.
  - **Кнопка "Hủy" (Отмена):** Отменяет изменения.
  - **Кнопка "Lưu tất cả thay đổi" (Сохранить все изменения):** Сохраняет все изменения в профиле.

### 7.4. Вкладка "Безопасность"

- **Заголовок:** "Đổi mật khẩu" (Сменить пароль).
- **Форма:**
  - **Поле "Mật khẩu hiện tại" (Текущий пароль):** Поле для ввода старого пароля.
  - **Поле "Mật khẩu mới" (Новый пароль):** Поле для ввода нового пароля.
  - **Поле "Xác nhận mật khẩu mới" (Подтвердите новый пароль):** Поле для повторного ввода нового пароля.
- **Кнопка "Lưu thay đổi" (Сохранить изменения):** Отправляет форму для смены пароля.

### 8. Иконка "Уведомлениями" в header
--открывает окно с уведомлениями
 